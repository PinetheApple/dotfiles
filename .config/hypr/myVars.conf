# Define variables

# apps
$terminal = gnome-terminal
$fileManager = thunar
$menu = rofi -show drun
$browser = flatpak run io.github.zen_browser.zen
$vpn = /opt/Mullvad VPN/mullvad-vpn
$clipboard = clipman pick -t rofi

# system commands
$suspend = hyprlock & systemctl suspend
$shutdown = systemctl poweroff

# key mods
$main = SUPER
$shift = SHIFT
